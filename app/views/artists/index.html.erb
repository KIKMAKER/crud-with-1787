<h1>All Artists</h1>

<% @artists.each do |artist| %>
  <p><%= artist.name %> from <%= artist.country %></p>
  <p><%= link_to 'See info', artist_path(artist) %> </p>
<% end %>


<%= link_to 'new artist', artists_new_path %>
